export default function PostDeliveryMoment({ onComplete }) {
  return (
    <div style={{ padding: 40 }}>
      POST DELIVERY — placeholder
      <br />
      <button onClick={onComplete}>Afronden</button>
    </div>
  );
}
